# App environment variables live next to the Next.js app.
#
# Copy the template and edit:
#   cp apps/nextjs/.env.example apps/nextjs/.env
#
# See apps/nextjs/.env.example for all keys (database, Better Auth, Stripe, etc.).
